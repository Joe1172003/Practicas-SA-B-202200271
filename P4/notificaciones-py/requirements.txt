# Las versiones van fijadas (== y no >=) para que la imagen se construya
# igual hoy que dentro de seis meses. Es el equivalente al package-lock.json
# de los servicios de Node.

# CAMBIO DE LA P5: se fueron fastapi y uvicorn. Este servicio ya no es un
# servidor web, asi que cargar un framework HTTP no tenia sentido. De paso
# la imagen queda mas liviana.

# Cliente de RabbitMQ. Es sincrono, que es justo lo que queremos: un
# mensaje a la vez, procesado con calma.
pika==1.3.2

# Driver de PostgreSQL. La version "binary" trae el compilado listo y evita
# tener que instalar herramientas de compilacion dentro de la imagen.
psycopg2-binary==2.9.10
