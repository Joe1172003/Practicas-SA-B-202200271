# Lo que NO se manda al construir la imagen.
# node_modules es lo importante: si se copiara desde Windows, traería binarios
# compilados para Windows dentro de una imagen Alpine (Linux) y reventaría.
node_modules
dist
.env
test
*.md
